// 스위프트에서 변수를 생성하려면 var키워드를 사용합니다.
// var [변수명]: [데이터 타입] = [값]

// 변수를 생성할 때 데이터 타입은 생략 가능 

var name: String = "yagom"
var age: Int = 100
var job = "iOS Programmer"
var height = 181.5

age = 99
job = "Writer"

print("저의 이름은 \(name)이고, 나이는 \(age)세이며, 직업은 \(job)입니다. 키는 \(height)입니다.")
